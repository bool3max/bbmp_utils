#pragma once

/* AIO system include, used by clients linking against the library */
/* All other header files are stored in the top-level bbmp folder, for categorization */

#include <bbmp/bbmp_parser.h>
#include <bbmp/bbmp_helper.h>
